# Server
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Database (MySQL + Drizzle)
DATABASE_URL=mysql://paypilot:paypilot_dev_pw@localhost:3306/paypilot

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d

# Internal endpoints (e.g. POST /internal/reminders/process)
INTERNAL_API_KEY=change-me-in-production

# --- Integrations (added when PayHub / Teekrr / Stripe credentials are provided) ---
# PAYHUB_BASE_URL=
# PAYHUB_API_KEY=
# TEEKRR_BASE_URL=
# TEEKRR_API_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
